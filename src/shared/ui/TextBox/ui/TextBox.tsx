import { TextAlign, TextSize } from "@/shared/ui/Text/ui/Text";

interface TextBoxProps {
    className?: string;
    label?: string;
    text?: string;
    variant?: TextVariant;
    align?: TextAlign;
    size?: TextSize;
    bold?: boolean;
}

const TextBox = () => {
    return <div></div>;
};

export default TextBox;
